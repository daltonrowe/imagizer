{
  "name": "imagizer",
  "version": "0.1.0",
  "private": true,
  "description": "In-browser image effect editor",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server -p 8000 .",
    "test": "node --test test/*.test.mjs"
  }
}
